{
  "id": "wormbase-silent-mode",
  "name": "WormBase Silent Mode",
  "description": "Claims openclaw's before_agent_reply hook when WORMBASE_SILENT_MODE is truthy so the built-in default agent never replies — gate 6 of the WormBase silent-mode design.",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
